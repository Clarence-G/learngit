import sys
import KNN
from numpy import *

sys.path.append("...文件路径...")

dataSet, labels = KNN.create_dataset()
inputt = array([1.1, 0.3])
K = 3
output = KNN.classify(inputt, dataSet, labels, K)
print("测试数据为:", inputt, "分类结果为：", output)
